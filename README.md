# Kaspi Shop Panel

Система для парсинга и аналитики цен товаров с Kaspi.kz. Позволяет отслеживать цены, анализировать конкурентов, генерировать отчеты и получать рекомендации по ценообразованию.

## 🚀 Возможности

- **Парсинг товаров** - Автоматический сбор данных о товарах и ценах с Kaspi.kz
- **Аналитика цен** - Детальный анализ цен, позиций и конкурентной среды
- **История цен** - Отслеживание изменений цен во времени
- **Сравнение по датам** - Сравнение цен на разные даты с генерацией отчетов
- **Генерация отчетов** - Excel отчеты по товарам, сравнениям и аналитике
- **Real-time обновления** - WebSocket для получения обновлений в реальном времени
- **Планировщик задач** - Автоматическое обновление цен по расписанию
- **AI аналитика** - Рекомендации по ценообразованию с использованием OpenAI
- **Дашборд** - Визуализация метрик и статистики системы

## 📋 Требования

- Docker и Docker Compose
- Git

## 🛠️ Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd kaspi-shop-panel
```

### 2. Настройка окружения

Создайте файл `.env` в корне проекта:

```env
# Database
DATABASE_URL=postgresql://kaspi_user:kaspi_password@postgres:5432/kaspi_db

# Redis
REDIS_URL=redis://redis:6379/0
REDIS_TTL=86400

# MinIO
MINIO_ENDPOINT=minio:9000
MINIO_PUBLIC_URL=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=kaspi-reports
MINIO_SECURE=false

# OpenAI (опционально, для AI аналитики)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Парсинг
MAX_RETRIES=3
PARSING_TIMEOUT=30
TOP_SELLERS_COUNT=10
MAX_CONCURRENT_JOBS=5
PARSING_INTERVAL_HOURS=24
PARSING_INTERVAL_MINUTES=0

# Database Pool
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_RECYCLE=3600
DB_POOL_TIMEOUT=30
```

### 3. Запуск проекта

```bash
docker-compose up -d
```

Это запустит все сервисы:
- **Backend** (FastAPI) - `http://localhost:8000`
- **Frontend** (React) - `http://localhost:3000`
- **PostgreSQL** - `localhost:5432`
- **Redis** - `localhost:6379`
- **MinIO** - `http://localhost:9000` (API), `http://localhost:9001` (Console)

### 4. Инициализация базы данных

База данных создается автоматически при первом запуске. Если нужно применить миграции:

```bash
docker-compose exec backend alembic upgrade head
```

### 5. Доступ к MinIO Console

- URL: `http://localhost:9001`
- Логин: `minioadmin`
- Пароль: `minioadmin`

## 📖 Использование

### API Документация

После запуска доступна интерактивная документация:
- **Swagger UI**: `http://localhost:8000/docs`
- **ReDoc**: `http://localhost:8000/redoc`

### Основные endpoints

- `GET /api/v1/products/` - Список товаров
- `POST /api/v1/products/` - Добавить товар
- `GET /api/v1/analytics/dashboard` - Метрики дашборда
- `GET /api/v1/reports/products/{id}/excel` - Отчет по товару
- `GET /api/v1/reports/products/{id}/price-comparison-excel` - Отчет сравнения цен

### Frontend

Откройте `http://localhost:3000` в браузере.

**Основные разделы:**
- **Дашборд** - Обзор системы, метрики, добавление товаров
- **Товары** - Список отслеживаемых товаров
- **Аналитика** - Детальная аналитика по товарам
- **Отчеты** - Генерация отчетов
- **Планировщик** - Настройка автоматического парсинга
- **Архив отчетов** - Просмотр сохраненных отчетов

## 🏗️ Архитектура

```
kaspi-shop-panel/
├── backend/
│   ├── app/
│   │   ├── api/v1/          # API endpoints
│   │   ├── core/            # Конфигурация, БД, Redis, MinIO
│   │   ├── models/          # SQLAlchemy модели
│   │   ├── schemas/         # Pydantic схемы
│   │   └── services/       # Бизнес-логика
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/
│   ├── src/
│   │   ├── components/      # React компоненты
│   │   ├── hooks/           # Custom hooks
│   │   ├── lib/             # Утилиты, API клиент
│   │   └── pages/           # Страницы приложения
│   ├── package.json
│   └── Dockerfile
└── docker-compose.yml
```

## 🔧 Технологии

### Backend
- **FastAPI** - Web framework
- **SQLAlchemy 2.0** - ORM
- **PostgreSQL** - База данных
- **Redis** - Кэширование
- **MinIO** - Хранение файлов
- **APScheduler** - Планировщик задач
- **Tenacity** - Retry логика
- **Prometheus** - Метрики
- **WebSockets** - Real-time обновления

### Frontend
- **React 18** - UI библиотека
- **TypeScript** - Типизация
- **React Query** - Кэширование и состояние
- **Tailwind CSS** - Стилизация
- **Radix UI** - UI компоненты
- **Recharts** - Графики
- **Vite** - Сборщик

## 📊 Основные функции

### Парсинг товаров
- Извлечение данных о товарах с Kaspi.kz
- Сбор информации о продавцах и ценах
- Автоматическое обновление по расписанию
- Retry логика при ошибках

### Аналитика
- Оценка позиции по цене
- Статистика цен (мин/макс/средняя)
- История изменений цен
- Сравнение цен по датам
- AI рекомендации по ценообразованию

### Отчеты
- Отчет по товару (Excel)
- Сравнение товаров (Excel)
- Сравнение цен по датам (Excel)
- Расширенная аналитика (Excel)
- Архив отчетов в MinIO

### Real-time
- WebSocket обновления статуса парсинга
- Автоматическое обновление списка товаров
- Уведомления о завершении задач

## 🔐 Безопасность

- Rate limiting на API endpoints
- Валидация входных данных (Pydantic)
- CORS настройки
- Environment variables для секретов

## 📈 Мониторинг

- **Health Check**: `GET /health` - Проверка состояния сервисов
- **Metrics**: `GET /metrics` - Prometheus метрики
- **Логирование**: Централизованное логирование в `logs/app.log`

## 🐛 Устранение неполадок

### Проблемы с подключением к БД

```bash
docker-compose logs postgres
docker-compose restart postgres
```

### Проблемы с Redis

```bash
docker-compose logs redis
docker-compose restart redis
```

### Пересборка контейнеров

```bash
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f backend
docker-compose logs -f frontend
```

## 🔄 Обновление

```bash
git pull
docker-compose down
docker-compose build
docker-compose up -d
```

## 📝 Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `DATABASE_URL` | URL подключения к PostgreSQL | `postgresql://kaspi_user:kaspi_password@postgres:5432/kaspi_db` |
| `REDIS_URL` | URL подключения к Redis | `redis://redis:6379/0` |
| `MINIO_ENDPOINT` | Endpoint MinIO | `minio:9000` |
| `MINIO_ACCESS_KEY` | Access key для MinIO | `minioadmin` |
| `MINIO_SECRET_KEY` | Secret key для MinIO | `minioadmin` |
| `OPENAI_API_KEY` | API ключ OpenAI (опционально) | - |
| `CORS_ORIGINS` | Разрешенные origins для CORS | `http://localhost:3000,http://localhost:5173` |
| `DB_POOL_SIZE` | Размер пула соединений БД | `10` |
| `DB_MAX_OVERFLOW` | Максимальный overflow пула | `20` |

## 🚀 Production Deployment

Для production рекомендуется:

1. Использовать переменные окружения для секретов
2. Настроить HTTPS
3. Использовать внешний PostgreSQL и Redis
4. Настроить резервное копирование БД
5. Настроить мониторинг (Grafana + Prometheus)
6. Добавить аутентификацию

## 📄 Лицензия

Проект для внутреннего использования.

## 🤝 Поддержка

При возникновении проблем проверьте:
1. Логи контейнеров: `docker-compose logs`
2. Health check: `http://localhost:8000/health`
3. API документацию: `http://localhost:8000/docs`

## 📚 Дополнительная информация

- API документация доступна по адресу `/docs` после запуска
- Все отчеты сохраняются в MinIO и доступны через архив
- WebSocket подключения автоматически переподключаются при обрыве
- Метрики Prometheus доступны по адресу `/metrics`

